# Upload track

The backend must provide an API endpoint so that a myTracks app can upload a track.

## Input

* Identifier for the track: unique string with max length of 256 characters
* Date of upload
* The raw content of the GPX file.

## Return

* HTTP status
